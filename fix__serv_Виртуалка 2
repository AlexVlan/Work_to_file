1.	На сервере не запускаются сервисы nginx, docker-engine и контейнер с php-fpm. 
Найти причину, устранить, запустить сервисы.
2.	Наладить связку nginx->php-fpm->postfix, т.к. вероятно она работает некорректно (проверить настройки msmtprc, в них указан неправильный ip адрес хостовой машины).
3.	Внести изменения в php скрипт на отправку почты, чтобы при получении почты, в поле from стоял личный адрес админа, решающего задание.
4.	выполнить GET запрос http://localhost/mail.php.
5.	сообщить о выполнении задания.
  
  1. Проверим работу nginx - отключен, попробуем запустить, ? запустился, странно. 
  Проверим докер - отключен, пробуем включить, включился, странно.
  Пробуем запустить  docker run php-fpm:5.6_09, не выходит, нехватка памяти
  Заняты все иноды, иноды использовала директория /root/.tmp с пустыми файлами. Для начала очищаем директорию, проверяем на автоматическое создание файлов в директории, 
  для этого перезагружаем сервер.
  Знал бы докер, жил бы в Сочи. Ищем логи. Не находим. Идем от простого, selinux отключен значит не он, iptables нет, включен firewalld, для теста отключаю, не он. Пробуем в докере 
  запустить hello world.


  
